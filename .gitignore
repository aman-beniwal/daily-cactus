# Secrets / local-only (NEVER commit) — token file for the deploy
GITHUB_TOKEN.local.md
*.local.md
*.local

# Local Claude Code settings (machine-specific; do not publish)
.claude/

# macOS
.DS_Store
**/.DS_Store

# Python
__pycache__/
*.pyc

publish_health.txt
trial/feeds/
trial_site/
newsroom_alert.txt
